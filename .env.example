# Better Auth: Base URL (update for production)
BETTER_AUTH_URL="http://localhost:3000"

# Better Auth: Secret key (generate with: openssl rand -base64 32 - never commit!)
BETTER_AUTH_SECRET=""

# Better Auth: Google OAuth credentials (from Google Cloud Console)
BETTER_AUTH_GOOGLE_CLIENT_ID=""
BETTER_AUTH_GOOGLE_CLIENT_SECRET=""

# Drizzle: PostgreSQL connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/linksly"
